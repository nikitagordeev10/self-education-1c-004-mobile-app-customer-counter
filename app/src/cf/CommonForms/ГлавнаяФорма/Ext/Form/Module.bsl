&НаКлиенте
Процедура УказатьКонтролера(Команда)
	Имя = "";
	ЗаголовокВопроса = "Введите имя";
	Если ВвестиСтроку(Имя, ЗаголовокВопроса, 0, Истина) Тогда
		ЗапомнитьИмя(Имя);
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ЗапомнитьИмя(Имя)
	ПараметрыСеанса.ТекущийПользователь = Имя;
	ИмяКонтролера = Имя;
КонецПроцедуры	

&НаКлиенте
Процедура СчитатьБрак(Команда)
	ОткрытьФорму("ОбщаяФорма.ФормаПодсчетаБрака");
КонецПроцедуры
